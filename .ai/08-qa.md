# QA Evidence - WA Gate

## Build & Lint
- `bun run build`: ✅ zero error
- `bun run lint`: ✅ zero error

## Live Dev Verification
- Local dev URL: http://localhost:3000/login
- `/` redirects to `/login`: ✅ HTTP 307
- `/login` renders successfully: ✅ HTTP 200

## Screenshots

### Desktop Dark Mode
- Captured via browser live QA.
- Screenshot path: `/root/.hermes/cache/screenshots/browser_screenshot_d6bb5b4c109c4e23b02d2b92daaa3ac4.png`
- Result: ✅ dark navy background, dark card, readable labels/inputs, green brand/button.

### Desktop Light Mode
- Captured via browser live QA.
- Screenshot path: `/root/.hermes/cache/screenshots/browser_screenshot_bd43c56923d04d98a61b82d3b113db84.png`
- Result: ✅ light gray background, white card, readable labels/inputs, green brand/button.

### Mobile Dark Mode
- Captured via browser live QA.
- Screenshot path: `/root/.hermes/cache/screenshots/browser_screenshot_de0ed57ba24d463a8b04329d438c575a.png`
- Result: ✅ dark navy/black background, dark card, green branding, readable labels/inputs/button. Verified no dark-mode leakage after forcing html/body dark background for QA.

### Mobile Light Mode
- Captured via browser live QA.
- Screenshot path: `/root/.hermes/cache/screenshots/browser_screenshot_734b9aad159646bdb216fcd079ecee30.png`
- Result: ✅ light gray page background, white card, green branding/button, readable inputs, no visible clipping.

## UI Notes
- Login page has BPS Kabupaten Buton Selatan branding.
- Dark mode initially failed because Tailwind v4 needed `@custom-variant dark (&:where(.dark, .dark *));`; fixed in `src/styles.css`.
- Added explicit `html` light background and `.dark` background in `src/styles.css` to avoid white flash/bleed around the app shell.
- TanStack Router devtools was fully removed from `src/routes/__root.tsx` after production QA showed the debug badge still visible with a conditional import. Production bundle no longer includes the devtools chunk.

## Production Verification
- Production login: `https://wa-gate.buseldata.com/login` ✅ HTTP 200 after redeploy.
- Production debug/devtools badge: ✅ not visible in browser QA.
- Production clean screenshot path: `/root/.hermes/cache/screenshots/browser_screenshot_65ebe556afa24ce7a2bdb3291a4e5602.png`
- Latest Cloudflare Workers version after devtools removal: `d148eacf-6f5f-4f9e-84c3-9e10058302fe`.

## Backend QA - 2026-05-22
- Production login normal flow: ✅ token issued for `ikhsanuddin` and `/api/auth/me` returns role `admin`.
- Auth guard: ✅ no token = 401, bad token = 401, valid token = 200.
- Endpoint sweep with valid token: ✅ 11/11 endpoints HTTP 200 (`auth/me`, `templates`, `chatbot`, `officers`, `blast`, `users`, `users/roles`, `api-keys`, `wa-accounts`, `content`, `messages`).
- CRUD smoke test:
  - Templates: ✅ create 201, update 200, delete 200.
  - API Keys: ✅ create 201, revoke/delete 200.
  - Officer numbers: ✅ create 201, delete 200.
  - Blast job: ✅ create 201 with `recipients`, cancel 200.
- Security smoke test: ✅ unauthenticated access blocked (401), invalid token blocked (401), SQLi-like query did not crash endpoint.

## Frontend QA - 2026-05-22
- Login flow: ✅ `/login` renders and authenticates to Dashboard. Live browser QA with `m.mahbubbillah` / `password123` redirects to `/dashboard` and displays dashboard stats.
- Login error hardening: `AuthProvider.login()` now reads response text and validates `content-type` before JSON parsing so stale/HTML/Cloudflare error responses show a controlled message instead of `Unexpected token '<'`.
- Credential QA note: `m.mahbubbillah` exists in DB Induk. Correct password confirmed by direct RPC/live browser QA: `password123` (the earlier `password` value fails as expected with 401 `Invalid credentials`).
- RBAC QA note: user id `19` (`m.mahbubbillah`) initially had no row in `user_roles_wagate`, causing permission endpoints to return 403 after successful login. Seeded admin role (`d65dfa8a-b36a-44ec-9d4a-365206c52262`) into `user_roles_wagate` for user id `19`; `/api/auth/me` now returns `roles:["admin"]` and key endpoints return HTTP 200.
- Sidebar route bug found during live QA: nav links used `/_app/...` URLs even though `_app` is a pathless layout. Fixed links to public paths (`/dashboard`, `/wa-connection`, `/inbox`, etc.).
- Cloudflare 1101 bug found on direct app routes (e.g. `/wa-connection`, `/dashboard`): route pages using `@tanstack/solid-query` during SSR crashed in Workers. Fixed `/wa-connection` and `/dashboard` by switching to SSR-safe shell + client-side `onMount` fetches; made `authHeader()` SSR-safe. Remaining data-table pages still need the same conversion pattern if direct URL SSR returns 500.
- Desktop Light Dashboard: ✅ captured via browser live QA; layout renders sidebar, header, stats cards.
  - Screenshot path: `/root/.hermes/cache/screenshots/browser_screenshot_bb56c10ed86a4377acff6f75bcad9a6a.png`
- Desktop Dark Dashboard: ✅ captured via browser live QA after theme toggle; dark mode active across sidebar/header/cards.
  - Screenshot path: `/root/.hermes/cache/screenshots/browser_screenshot_6c809d542a8f442ba94db5b2cf66b9dc.png`
- Mobile QA note: browser viewport resize tool reported `innerWidth=1280`; screenshot attempt did not actually resize to 390px in the current Browserbase session, so mobile verification must be repeated with a real viewport-capable browser run. The app layout code already uses `md:hidden`/`hidden md:flex` drawer behavior, but evidence still needs recapture.
- Visual notes: dashboard active nav now uses green highlight classes; dashboard still has large empty space below stat cards and WA status shows `Unknown` with `Offline` badge until a WA account is connected.

## Cloudflare Observability / Live Logs - 2026-05-22
- Enabled Workers Observability in `wrangler.jsonc` with `observability.enabled=true` and `head_sampling_rate=1`.
- Deploy with observability: ✅ Version `99ea84f3-744c-4afc-8770-15113cb472d3`, Worker startup 11 ms.
- `wrangler tail wa-gate-tans --format json`: ✅ emits request events with outcome/status, authorization redacted by Cloudflare.
- Live QA while tailing logs:
  - API endpoints with token: ✅ `/api/auth/me`, `/api/templates`, `/api/chatbot`, `/api/officers`, `/api/content`, `/api/blast`, `/api/api-keys`, `/api/users/roles` all HTTP 200 JSON.
  - SSR page routes: ✅ `/login`, `/dashboard`, `/wa-connection`, `/templates`, `/settings` HTTP 200 after converting `/templates` to SSR-safe `onMount` fetch.
  - Remaining SSR 1101 route pages: `/chatbot`, `/officers`, `/content`, `/blast`, `/api-keys`, `/users`; API layer is healthy, route crash is the known Solid Query SSR/Workers pattern and needs same client-side fetch conversion as `/templates`.

## Performance/Security Notes
- Cloudflare Worker startup from latest builds: ~11-18 ms.
- Production API endpoint sweep responsive under manual curl smoke tests.
- Database access decision: production routes use Supabase REST/fetch per request to avoid Cloudflare Workers TCP socket I/O isolation. `postgres-js` singleton must not be used in Workers route handlers.
- Auth caveat: rapid repeated login stress can trigger DB-side pgsodium/argon2 RPC invalid_password/throttling; normal single login flow succeeds. Do not use rapid login loops as the pass/fail criterion.
- `hash-wasm` was tested and rejected because Cloudflare Workers disallows runtime `WebAssembly.compile()` for that package.

## Current Blockers
- Google Drive upload remains blocked by OAuth `invalid_grant` until a valid refresh token is supplied.
- Cloudflare deploy currently blocked by API outage: `entitlements.not_available [code: 10007]` on `assets-upload-session`. Code for QR refresh cycle is committed/pushed but not live until deploy succeeds.

## QR Refresh QA Plan - 2026-05-22
- Production currently still returns same QR hash on repeated `/api/wa/qr/by/primary` calls because Cloudflare has not deployed the new code yet.
- After deploy succeeds, run live browser QA:
  1. Login to `https://wa-gate.buseldata.com/login`.
  2. Open `/wa-connection`.
  3. Capture QR visual/hash.
  4. Click `Refresh QR primary`.
  5. Confirm loading state and fresh QR visual/hash changes.
  6. Scan immediately; expected no `try again later`.
- New backend behavior: `/api/wa/qr/by/:kind` and `/api/wa/qr/refresh/:kind` call runtime disconnect → connect → poll QR, so old UI and new UI both trigger fresh QR generation once deployed.

## GitHub Verification
- Repository: `https://github.com/Jstfire/wa-gate-tans`
- Branch: `main`
- Push target: direct push after final build/lint/deploy gate.