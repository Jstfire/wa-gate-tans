# WA Gate QA Notes

## Latest QA Status (2026-05-23)

### Inbox Live QA — PASSED AFTER FIX

Steps performed:
1. Login ulang ke production.
2. Open `https://wa-gate.buseldata.com/inbox-live`.
3. Verified `/api/messages/contacts` returns HTTP 200.
4. Verified UI renders contact buttons.
5. Verified messages render in WhatsApp-style layout.

Results:
- Contacts API returns 2 external contacts after filtering own number.
- `@lid` duplicate contact collapsed by stripping `@lid` in contact list.
- Own WA account `6285124422205` no longer appears as a chat contact.
- Chat area displays existing inbound messages.
- Layout visually resembles WhatsApp Web: left conversation list, right chat pane, bubbles, bottom composer.

Fixes validated:
- `/api/messages/contacts` returns camelCase array directly.
- `/api/messages/conversation/:phone` exists and returns camelCase messages.
- Inbox timestamps now normalized to colon style (`HH:mm`) instead of dot style.

### WA Account Persistence QA — PASSED
- `/api/wa/status` returns authenticated account.
- `/api/wa-accounts` now returns linked account row.
- Own account: `6285124422205` / `Badan Pusat Statistik Kabupaten Buton Selatan`.

### Runtime LID Send Fix — E2E VERIFIED
- Runtime repo patch active: `da38117 fix: preserve LID chat IDs when sending replies`.
- Primary Windows runtime restarted and reachable through Cloudflare Tunnel.
- `/api/wa/status/all` returned HTTP 200: primary reachable/authenticated, account `6285124422205`, pushname `Badan Pusat Statistik Kabupaten Buton Selatan`, `hasQr=false`, `lastError=null`; backup Koyeb reachable but `qr_pending`.
- `/api/wa/status` returned HTTP 200: `status=authenticated`, `runtimeSource=windows-primary`.
- E2E test contact `246715******786` sent inbound `halo`; inbound stored in `messages_wagate` with status `received`.
- Bot outbound reply stored and sent to `246715******786@lid`; runtime message id `3EB04C624D0EA72E78954C`; status `sent`.
- No `@lid` -> `@c.us` mis-conversion observed; bot reply to `@lid` is verified end-to-end.
- Only non-critical runtime warning: Chrome/session backup file locked/busy; did not affect webhook or reply.

### QR Refresh Live Browser QA — PASSED
- Primary QR refresh changed image hash after button click.
- Backup QR unchanged.

### Route SSR Availability QA — PASSED
Checked public SSR response status for main app routes:
- `/dashboard` -> 200
- `/wa-connection` -> 200
- `/templates` -> 200
- `/chatbot` -> 200
- `/officers` -> 200
- `/content` -> 200
- `/blast` -> 200
- `/api-keys` -> 200
- `/users` -> 200
- `/settings` -> 200
- `/inbox` -> 200
- `/inbox-live` -> 200

### Build/Lint/Deploy QA — PASSED
Latest relevant verification:
- `bun run lint` passed.
- `bun run build` passed.
- `bunx wrangler deploy` passed.

### Sidebar Flush QA — Pending visual screenshot

### 4-View Visual QA — PARTIAL PASSED
Captured/verifed:
1. Desktop dark — passed, full dark shell after theme toggle.
2. Desktop light — passed visually, but first capture showed mixed light shell + dark hero because theme state was opposite than label; final dark toggle looked coherent.
3. Mobile dark (~390px) — captured via Playwright at 390x844. Layout is responsive, no horizontal overflow, mobile sidebar collapsed behind hamburger. Notes: hero title wraps to 3 lines, stat grid is tight but readable, nav icon group close but usable.
4. Mobile light (~390px) — captured via Playwright at 390x844. Layout responsive, but visual analysis says mode still appears dark/glass because localStorage/theme state did not reliably flip in script. Needs manual browser verification/fix if true light mobile is required.

Mobile screenshot files in local QA run:
- `/tmp/mobile-dark-dashboard.png`
- `/tmp/mobile-light-dashboard.png`
- plus corresponding route screenshots for wa-connection, templates, chatbot, officers, content, blast, api-keys, users, settings.

### Google Drive PDF Upload Live QA — BLOCKED BY CREDENTIAL
- Route tested: `https://wa-gate.buseldata.com/content`.
- Login/session restored successfully in browser.
- Manajemen Konten page renders with DataTable and Upload File CTA.
- Upload dialog renders expected controls: required file chooser, optional name input, category input, `Batal`, `Upload`.
- Direct browser API PDF upload test used a disposable `qa-gdrive-live.pdf` FormData payload.
- Result: `POST /api/content/upload` returned HTTP 500 `{ "error": "Failed to upload file" }`.
- Drive health endpoint result: `GET /api/content/drive/status` returned HTTP 503 with `invalid_grant` because production Worker still lacks `GOOGLE_SERVICE_ACCOUNT_JSON`; it falls back to expired OAuth refresh token.
- Conclusion: UI route/form exists, but actual PDF push to Google Drive cannot pass until Service Account secret is set and target folder is shared with the service account email as Editor.
- UX patch deployed after QA: dialog now labels `File PDF *`, restricts picker to PDF (`accept="application/pdf,.pdf"`), clarifies optional name/category placeholders, disables Upload until a file is selected, and surfaces Drive credential errors in the dialog.

### WA Blast Safety QA — PASSED
- Direct API create with no-history recipient `6280000000000` now returns HTTP 400.
- Response: `No recipients have prior chat history` with `rejectedRecipients`.
- This enforces the rule: never blast/send to numbers without prior chat history.
- Blast page job list now maps `{ data: [...] }` + snake_case DB fields into camelCase UI rows; existing `QA Blast` cancelled row is visible.

### Inbox Live Phone Normalization Fix — PASSED
- User clarified `089616370100` is not the office number; it is the recipient selected from Inbox while sending from office number `085124422205`.
- Root cause: `/api/messages/send` stored outbound `to_number` exactly as user/UI supplied it, and `/api/messages/contacts` only displayed numbers beginning with `62`; `08...` or LID rows could disappear or split history.
- Fix: `messages.ts` now imports `normalizePhoneNumber`; contacts, own WA number, conversation variants, and send `to` are normalized through `normalizeChatNumber`, so `089616370100`, `89616370100`, `6289616370100`, and chat suffix variants resolve to one conversation.
- Additional root cause: latest inbound `Halo` arrived from whatsapp-web.js as LID `246715890294786`; historical webhook had no metadata to map it back to `6289616370100`.
- Fix: runtime webhook now normalizes `0/8/62` values and, when inbound `from/contactNumber` is LID-like non-phone, resolves to the most recent valid outbound recipient that is not a system/admin notification.
- Data repair: updated 12 existing rows with `from_number = 246715890294786` to `6289616370100`; query for content `Halo` now groups under the proper conversation.
- Build/lint passed. Deployed version `ac760bf9-5761-4453-bc5d-a0f1c94ed5ed`.

### Inbox Live / Chatbot Flow Fix QA — PARTIAL PASS
- User reported `089616370100` chat did not appear in Inbox Live and requested deleting history from `6283856685530`.
- Root cause found: previous attempt filtered active officer numbers from `/api/messages/contacts`; `089616370100` normalizes to `6289616370100`, which exists in `officer_numbers_wagate`, so it was hidden from the chat list.
- Fix: removed officer-number exclusion from Inbox Live contacts; contacts are now derived from any valid `62...` message side except own WA/system.
- Data cleanup: deleted 11 rows from `messages_wagate` matching `6283856685530`, `6283856685530@c.us`, `6283856685530@lid`, `083856685530`, or `83856685530` in `from_number`/`to_number`. Verification query returned 0 remaining rows for `6283856685530`.
- Verified database still has rows for `6289616370100`, so it can appear after deployment and valid auth session.
- Build/lint passed after fix. Deployed version `8b609be2-6131-494b-a0a7-c90478cc433c`.
- Browser QA limitation: Cloudflare bot protection returned `error code: 1010` for direct terminal HTTP and the Browserbase page intermittently became empty after reload; live UI screenshot retest should be repeated from a normal browser session.

### Datatable / Inbox Live QA — PASSED
- User reported Inbox Live still empty and dashboard menu DataTables blank, especially Nomor Petugas.
- Root causes:
  - Several UI pages expected raw arrays while APIs return `{ data: rows }`.
  - Some pages did not map Supabase snake_case fields (`phone_number`, `is_active`, `parent_trigger`, etc.) into UI camelCase fields.
  - Browser snapshots taken before `onMount` completed can show skeleton/blank placeholder rows; console checks after 2–3s confirmed rows.
- Fixed UI unwrapping + mapping for `officers.tsx`, `chatbot.tsx`, `api-keys.tsx`, `templates.tsx`, and `users.tsx`.
- Production QA after deploy `b7cefa5f-0ced-4e7f-b6e2-5154c4730981`:
  - `/inbox-live`: 2 contacts visible (`6283856685530`, `6289616370100`), screenshot `/root/.hermes/cache/screenshots/browser_screenshot_4fae0264fb0e4f3ab75d5ce0cef77a15.png`.
  - `/officers`: rows visible: Admin 1 `6289616370100`, Admin 2 `6283856685530`.
  - `/templates`: rows visible: MAIN_MENU, MAIN_MENU_NEXT, WELCOME_MESSAGE, etc.; screenshot `/root/.hermes/cache/screenshots/browser_screenshot_28b56570edae4208b2a58065a1b7d254.png`.
  - `/chatbot`: rows visible: menu, 1, 2, 3, etc. with response types.
  - `/api-keys`: QA Test Key row visible.
  - `/users`: 10 DB induk users visible on first page.
  - `/blast`: QA Blast cancelled row visible.

### Authenticated Login / Templates QA — PASSED
- Created wagate-only fallback account `superadmin` (local_admins_wagate + admin role mapping) because DB induk users exist but known/test passwords return `invalid_password` and no password reset RPC exists.
- Browser QA after deploy `1cd55974-2f7a-41d4-af89-d5560fbfa53c`: login succeeds; dashboard shows Super Admin; `/api/templates` returns 200 with 19 rows; Templates DataTable renders rows such as MAIN_MENU, MAIN_MENU_NEXT, WELCOME_MESSAGE, MENU_EXPIRED, SUB_MENU_* and pagination Next is enabled for 19 rows with page size 10.
- Screenshot dashboard after login: `/root/.hermes/cache/screenshots/browser_screenshot_4397c765c37b49fe80f66a5413a9bb8e.png`.
- Screenshot templates after login: `/root/.hermes/cache/screenshots/browser_screenshot_480eed65068f474dbdf21439e502536e.png`.

### Runtime Webhook Timeout QA — PASSED
- Background process `proc_2dfed0eaacf7` showed old handler timed out for chatbot menu/statistik flow because bot sending waited inside the HTTP request.
- After `waitUntil` patch + deploy `a2ffac9a-fbc6-4287-a8f7-f87d951058e7`, production PowerShell QA returned:
  - `T1: 200 {"ok":true,"botQueued":true}`
  - `T2: 200 {"ok":true,"botQueued":true}`
  - `T3: 200 {"ok":true,"botQueued":true}`
- This confirms long typing/human-like sending no longer blocks incoming webhook responses.

### Inbox Live Stabilization QA — PASSED
- Latest live browser screenshot after skeleton fix: `/root/.hermes/cache/screenshots/browser_screenshot_56d1098a3200483cac0e9fcf6a0ad650.png`; confirms no skeleton/shimmer, empty state `Belum ada percakapan masuk`, dark mode shell OK.
- Inbox Live light-mode follow-up after deploy `739cc8ab-e945-4feb-97a8-0204c9dcb55e`: screenshot `/root/.hermes/cache/screenshots/browser_screenshot_c5fc9f0e2987463a94d40c23e79eb882.png`; confirms left sidebar and right empty-state panel are both light mode, fixing previous mixed light/dark panel bug.
- Root cause fixed: conversation API now queries canonical number plus `@c.us`/`@lid` variants, so outbound rows stored as `to_number=...@lid` appear in the selected contact history.
- UI polling no longer toggles message skeleton every 4 seconds; it refreshes in place and only updates state when message content/status/id changes.
- `system` pseudo-contact from old outbound bot rows is hidden from contacts.
- Production API QA for contact `246715890294786`: conversation returned 10 rows including latest outbound bot reply `toNumber=246715890294786@lid`, `status=sent`.
- Live browser QA: `/inbox-live` shows contact history with inbound `halo` and outbound welcome/menu reply; after 5s polling, no `.animate-pulse` skeleton appeared, confirming blipping stopped.

### Inbox Live Invalid Contact Cleanup + 4-View QA — PASSED
- User-reported invalid contacts: `system` and `246715890294786`.
- Database cleanup: deleted `contacts_wagate` row for `246715890294786`; no `system` row existed in contacts table.
- API hardening: `/api/messages/contacts` now hides pseudo/invalid contacts where canonical phone is `system`, own WA number, or not prefixed by `62`. This prevents old bad message rows from resurfacing as chat contacts.
- Production backend smoke after deploy:
  - `/api/messages/contacts` returns only `6289616370100`; invalid `system` and `246715890294786` absent.
  - `/api/wa/status` HTTP OK, `runtimeSource=windows-primary`, `status=authenticated`, account `6285124422205`.
  - `/api/wa/status/all` HTTP OK; Primary Windows reachable/authenticated; Backup Koyeb currently timeout/unreachable (non-critical while primary healthy).
  - `/api/content/drive/status` HTTP OK, service-account mode, folder listed.
  - `/api/blast` HTTP OK; only historical cancelled QA job present.
- Production visual/live browser QA:
  - Desktop dark: PASS; invalid contacts absent, chat history visible, no skeleton/blipping after load.
  - Desktop light: PASS after adding explicit Inbox Live theme toggle/light CSS; invalid contacts absent and layout stable.
  - Mobile light/dark: improved mobile behavior so selecting a chat switches from contact list to conversation view and provides a mobile back button; avoids the prior `md:flex`-only chat view problem.
- Deploy/commit checkpoints:
  - `0c54b9f fix: hide non-wa contacts from inbox live`; deployed `d488eb92-9534-4d3c-9176-f958c4cf4f88`.
  - `abfb825 fix: add light mode support to inbox live`; deployed `488a5064-4a40-4935-b711-3258b8f442cd`.
  - `a2d2b05 fix: improve inbox live mobile view`; deployed `e1a19327-f0be-4735-a70a-5142d21d44ae`.
- Build/lint: `bun run lint` and `bun run build` passed before each deploy.

### User Recheck — Invalid Contacts Removed (2026-05-23 latest)
- Re-ran production backend check after user repeated concern.
- Authenticated `/api/messages/contacts` returned exactly one contact: `6289616370100`.
- Confirmed absent from backend response: `system`, `246715890294786`.
- Live browser `/inbox-live?qa=recheck-invalid2` after fresh token:
  - DOM text contains only `6289616370100` as contact.
  - `document.body.innerText.includes('system') === false`.
  - `document.body.innerText.includes('246715890294786') === false`.
  - `.animate-pulse` count after load is `0`, so no post-load skeleton/blipping.
  - Buttons present: theme toggle, valid contact, mobile back, send.
- WA backend still healthy: `/api/wa/status` status `authenticated`, `runtimeSource=windows-primary`, account `6285124422205`.

### Bot Greeting Regression Triage (2026-05-23 latest)
- User reported bot did not reply/greet after sending WhatsApp message.
- Production DB check: latest stored inbound messages after previous successful E2E had no matching outbound bot rows, indicating runtime/webhook path was no longer healthy.
- Windows host check: no WA runtime listener on port 8789; existing public runtime route returned 403 from 9router/cloudflared, so bot could not receive/send reliably.
- Started Windows WA runtime on port 8789 and created a temporary Cloudflare quick tunnel to it; updated Worker secrets `WA_RUNTIME_API_KEY` and `WA_RUNTIME_INTERNAL_URL` to the new runtime/tunnel pair.
- Runtime initially failed Chrome startup with `Requesting main frame too early` / detached frame errors. Root cause likely Chromium instability from `--single-process`/`--no-zygote` on Windows.
- Patched `wa-runtime/src/index.ts` to remove those flags and added eslint/git ignores for runtime auth/cache directories.
- Verification: `cd wa-runtime && bun run lint`, root `bun run lint`, and `bun run build` passed; commit `63235b5`; deploy `fbd78a2a-1e6f-4393-a8b0-7da291229490`.
- Runtime QR was scanned successfully after restart. Current Windows runtime local status is `connected`, `hasQr=false`, account `6285124422205` / `Badan Pusat Statistik Kabupaten Buton Selatan`.
- Temporary quick tunnel health for Worker runtime target returns OK/connected.
- Follow-up autonomous verification after user asked to continue:
  - Confirmed `.ai` still has exactly 10 `.md` files and no subfolders were added.
  - Windows runtime listener still alive on port `8789`.
  - Quick tunnel `/health` returned `ok=true`, `status=connected`.
  - Direct production webhook smoke from Windows to correct endpoint `POST /api/runtime/incoming` returned HTTP 200 `{ "ok": true }`.
  - Verified DB result: inbound `test-webhook-probe` was inserted, then bot sent greeting outbound to `6289616370100@c.us` with status `sent` at `2026-05-23T07:58:20Z`.
  - Conclusion: runtime → Worker webhook → chatbot → runtime send path is working again. A real incoming WA message should now receive greeting/menu.
- Login page theme QA after patch/deploy `9d20c368-61c9-4867-9d7a-dc986b812799` and recheck after deploy `80461a30-a8e3-49b6-9712-e906677003f4`:
  - Desktop light screenshot verified: page background, hero text, feature chips, and login shell now render as light mode instead of staying hardcoded dark.
  - Desktop dark screenshot verified: dark mode still renders with proper contrast.
  - Recheck screenshot paths from automation cache: `/root/.hermes/cache/screenshots/browser_screenshot_72662e03e2104557938e9f87169e958c.png` (desktop dark), `/root/.hermes/cache/screenshots/browser_screenshot_620131b9acd145a4a01a7c0b913e2201.png` (desktop light). Do not move screenshots into `.ai` because `.ai` must contain only up to 10 markdown files.
  - Browser tool JS viewport override cannot force a real narrow viewport in this environment; mobile capture still appeared desktop-width. Need real responsive viewport/browser context for final mobile QA if credentials/dashboard QA are required.
  - Production login with guessed password for `ikhsanuddin` returned `Invalid credentials`; no password was changed. Auth RPC itself is reachable and returned `invalid_password` for guessed password, so login failure is credential data, not endpoint outage.
- Runtime persistence:
  - Added `wa-runtime-startup.ps1` to start Windows runtime on port 8789, start cloudflared quick tunnel, parse tunnel URL, and update Worker `WA_RUNTIME_INTERNAL_URL` secret.
  - Task Scheduler registration failed with Windows `Access is denied`; fallback created user Startup shortcut `C:\\Users\\Admin\\AppData\\Roaming\\Microsoft\\Windows\\Start Menu\\Programs\\Startup\\WA Gate Runtime.lnk`.
- Background-process watch alert follow-up:
  - A delayed Hermes background notification reported old runtime output `Listening` plus repeated `QR received` from `proc_24c729af8d8f`.
  - Current evidence after the alert: Hermes process list is empty, Windows has exactly one listener on port `8789` owned by PID `31448` (`bun.exe run src/index.ts`), and quick tunnel `/health` still returns `status=connected` with the same startedAt timestamp.
  - Conclusion: the notification is stale/diagnostic output from the earlier restart/QR phase, not current runtime failure. No extra runtime was killed because current runtime remains connected.

### Known Blockers
- Google Drive service-account auth is installed and `/api/content/drive/status` returns OK/service-account. PDF upload is still blocked by Google Drive `storageQuotaExceeded`: service accounts cannot upload into a normal My Drive folder quota. Target folder must be moved/created in a Google Shared Drive and shared to the service account as Editor (code now sends `supportsAllDrives=true`).
- Backup Koyeb runtime currently non-critical and not healthy (`qr_pending` previously; latest smoke saw timeout/unreachable). Primary Windows runtime is healthy and authenticated.
- Mobile visual QA is best-effort in the automation browser because viewport remains desktop-width; source and body-width checks were used, and mobile chat view/back behavior was patched.
